#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>
int main()
{
	int i;
	printf("Hello world!/n");
	for(i=0;i<100;++i)
	{
		//printf("%d  /n",i);
		printf("/r%d ",i);              // /r是回车符，定位到一行的开始位置
		fflush(stdout);
	        sleep(1);
	}
	return 0;
}
